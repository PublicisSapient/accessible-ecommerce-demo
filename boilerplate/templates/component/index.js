const {{component}} = (function() {
  function public(path, selector) {
    // insert contents
  }

  function private(components) {
    // insert contents
  }

  return {
    public: public
  }
}());

export default {{component}};